cmake_minimum_required(VERSION 3.10.2)
project("cross")
# include_directories(export_include)
add_library(${PROJECT_NAME} ${LIBRARY_TYPE} cross.cpp)
find_library(log-lib log)
target_link_libraries(${PROJECT_NAME} ${log-lib} url_signature)